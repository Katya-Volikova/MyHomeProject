package main

func BuildRoom() {
	infoAboutNeighbours(initializeNeighbours())

	// TODO доделать функции вывода объектов комнаты
	// createRoom().infoAboutRoom(initializeItems(), initializeEquipment())
}
